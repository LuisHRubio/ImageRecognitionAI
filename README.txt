-------------------------------------------------------------------------------------
HOW TO INSTALL
-------------------------------------------------------------------------------------
1- Clone or Download the repository
2- Open a terminal within the root directory
3- Install the necessary libraries using "conda install --file requirements.txt" | (If you want to use pip you can use "pip install -r requirements.txt")
4- Once finished, run the following command in the terminal "python .\server.py"
-------------------------------------------------------------------------------------